spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/smart_expense_tracker_db
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true